We don't have malloc_usable_size. Therefore and probably for other reasons too we
need to use jemalloc. Not sure now with enabling (is it needed with the patch below?).
With this we don't have any warnings in about:memory.

ac_add_options --enable-jemalloc
ac_add_options --enable-replace-malloc

--- firefox-66.0.3/memory/build/mozjemalloc.cpp	2019-04-28 18:51:04.714759650 +0000
+++ firefox-66.0.3/memory/build/mozjemalloc.cpp.new	2019-04-28 18:53:02.137511250 +0000
@@ -1758,7 +1758,7 @@ static bool pages_purge(void* addr, size
 #      define JEMALLOC_MADV_PURGE MADV_FREE
 #      define JEMALLOC_MADV_ZEROS force_zero
 #    endif
-  int err = madvise(addr, length, JEMALLOC_MADV_PURGE);
+  int err = posix_madvise(addr, length, JEMALLOC_MADV_PURGE);
   return JEMALLOC_MADV_ZEROS && err == 0;
 #    undef JEMALLOC_MADV_PURGE
 #    undef JEMALLOC_MADV_ZEROS
@@ -2451,7 +2451,7 @@
         mStats.committed -= npages;
 
 #ifndef MALLOC_DECOMMIT
-        madvise((void*)(uintptr_t(chunk) + (i << gPageSize2Pow)),
+        posix_madvise((void*)(uintptr_t(chunk) + (i << gPageSize2Pow)),
                 (npages << gPageSize2Pow), MADV_FREE);
 #ifdef MALLOC_DOUBLE_PURGE
         madvised = true;
--- firefox-66.0.3/js/src/gc/Memory.cpp	2019-04-09 21:59:58.000000000 +0000
+++ firefox-66.0.3/js/src/gc/Memory.cpp.new	2019-04-28 19:16:06.739885737 +0000
@@ -774,7 +774,7 @@ bool MarkPagesUnused(void* region, size_
 #elif defined(XP_DARWIN)
   return madvise(region, length, MADV_FREE) == 0;
 #else
-  return madvise(region, length, MADV_DONTNEED) == 0;
+  return posix_madvise(region, length, MADV_DONTNEED) == 0;
 #endif
 }
 
